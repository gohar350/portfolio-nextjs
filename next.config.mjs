/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "racingandsports.co.uk",
      "www.betwayscores.com",
      "media.licdn.com",
    ],
  },
};

export default nextConfig;
